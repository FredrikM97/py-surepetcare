# Suggestion
* Separate to allow store data in .env file. This is optional feature and requires the --store argument. Otherwise just print result. If --store is active a prompt to select argument should appear
* If possible, share one command for setters/getters. Example is lid_delay.
* Todo add more conditions or requirements
